name: guest_app_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.8

dependencies:
  audioplayers: ^6.5.1
  flutter:
    sdk: flutter
  flutter_sound: ^9.30.0
  http: ^1.6.0
  permission_handler: ^12.0.1
  record: ^6.2.0
  shared_preferences: ^2.5.4
  web_socket_channel: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
